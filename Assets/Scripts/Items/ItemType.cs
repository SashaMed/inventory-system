public enum ItemType
{
    ClickUpgrade,   // Adds effectValue to coins per click while active
    Generator,      // Adds effectValue to coins per second while active
    Multiplier,     // Multiplies all income by effectValue while active
    Shield          // Pauses coin drain while active
}
